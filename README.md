# What is this?
Some `epub` files do not "just work" with sending them to Kindle. Whether it is done via
send-to-kindle webpage by Amazon or using Calibre and sending them over USB.

One of the problems is (a hand wavy explanation) is that the images are too big.
This script tries to fix this problem.